name: Ola Mundo
on: workflow_dispatch
jobs:
  minha-tarefa:
    runs-on: ubuntu-latest
    steps:
      - name: Dizer data
        run: date 
      - name: Dizer commit 
        run: echo "Commit feito por ${{ github.actor}}"
